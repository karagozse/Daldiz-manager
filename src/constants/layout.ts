/**
 * Layout constants used by the app shell (BottomNav, App wrapper).
 * --bottom-tab-height is set from BOTTOM_TAB_HEIGHT_PX so content can use
 * height: calc(100dvh - var(--bottom-tab-height) - env(safe-area-inset-bottom)).
 */

export const BOTTOM_TAB_HEIGHT_PX = 80;
